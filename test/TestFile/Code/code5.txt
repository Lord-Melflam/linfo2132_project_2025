final x int = 10;
final y float = x / 0;  $ Division par zero dans une constante (interdit)

a int = 5;
b int = a[0];  $ a n'est pas un tableau, l'indexation est invalide

p Point = Point(1, 2);
free p;
writeln(p);  $ Acces a un objet apres sa liberation (comportement indefini)

fun test(int v) int {
    return test(v);  $ Recursion infinie sans condition d'arret
}

fun main() {
    arr int[] = array [-5] of int;  $ Taille de tableau negative (interdit)

    writeln(chr(300));  $ Conversion invalide, valeur hors plage pour un caractere

    test(5);
}
